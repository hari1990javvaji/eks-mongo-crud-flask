apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongo-flask-app.fullname" . }}
  labels:
    {{- include "mongo-flask-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongo-flask-app.selectorLabels" . | nindent 4 }}
